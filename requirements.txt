# Requirements for AWS SageMaker Training
#
# For local development (running submit_sagemaker_job.py):
# pip install -r requirements.txt
#
# For the SageMaker container, dependencies are installed via Dockerfile

# AWS SDK and SageMaker
boto3>=1.28.0
sagemaker>=2.180.0

# Optional: For local testing and development
awscli>=1.29.0
